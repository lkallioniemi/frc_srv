if ($host ~* herokuapp\.com$) {
    rewrite                   ^/robots.txt$   /herokuapp-robots.txt   break;
}

index index.php;

location / {
    try_files                 $uri $uri/ /index.html?$args;
}

location /api/v1/ {
    try_files                 $uri $uri/ /api.php?request=$args;
}
